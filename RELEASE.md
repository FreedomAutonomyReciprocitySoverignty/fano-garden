# Release Notes: v1 Closure

## Status
v1 contract is closed and enforced by deterministic interop gates.

## Tags to create
- `browser-v1.0.0`
- `posix-haskell-v1.0.0`

## Included Guarantees
- deterministic canonicalization
- stable hash-chain and merkle behavior
- optional `lc`/`merkle` backward compatibility
- bidirectional runtime interop vectors
- signature and replay parity suites with negative cases

## Operator Note
Tagging is a maintainer action and should be performed only after all required CI checks are green on `main`.
